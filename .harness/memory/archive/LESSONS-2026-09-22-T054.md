# LESSONS archivadas — 2026-09-22 (T-054)

Curación de `.harness/memory/LESSONS.md` (LESSONS.md llegó al 80% de su tope,
que dispara curación automática). Se fusiona además el par duplicado
T-021/T-016b (ambas lecciones enseñaban a confirmar si un warning de
lint/eslint era preexistente) en una sola entrada `[build] (x2,
T-021/T-016b)` dentro de `LESSONS.md`. Se archivan íntegras, sin editar su
texto, las 2 lecciones más antiguas cuyo tema ya no aporta información nueva:
la de T-013 sobre `getByRole('group', { name })` para MoneyValue porque ya
quedó codificada como convención vigente en `DECISIONS.md` (D-069, que
sustituyó a D-005/D-006/D-007) y la de T-016a porque en ~37 tareas
posteriores ningún otro helper de render repitió el problema de identidad
inestable del valor de contexto. Ver `.harness/memory/LESSONS.md` para las
lecciones vigentes y `DECISIONS.md` (D-069) para la convención de MoneyValue.

- [test] (x1, T-013) Con MoneyValue/MoneyDisplay el símbolo y el número son spans distintos: getByText('$360') ya no encuentra nada (el matcher por defecto solo une los nodos de texto DIRECTOS). Asertar con getByRole('group', { name }) + toHaveTextContent.
- [test] (x1, T-016a) Un helper de render compartido que inyecta un contexto debe crear el valor UNA vez por llamada (fuera del Wrapper) y envolver sólo los children: identidad estable evita re-registros en bucle y el provider propio de un test gana por anidamiento.
