# Handoff T-016b — Caja: el repositorio ya no ofrece política de caché y todo va a la red

## Interfaz expuesta
- `RegisterRepository.getRegisters(locationId: string): Promise<Register[]>` — **se fue el
  segundo parámetro** (`opts?: { force?: boolean }`). `ApolloRegisterRepository` usa
  `fetchPolicy: 'network-only'` siempre, igual que `getCajaStatus` ([D-017]). Las evicciones de
  `registers` + `posCajaStatusHome` tras `openSession`/`closeSession` se conservan tal cual (son
  para los OTROS consumidores del mismo cliente Apollo: el gate del shell, Hoy).
  `InMemoryRegisterRepository.getRegisters(_locationId)` (mock) sigue la misma firma.
- `useRegister(locationId)` devuelve lo mismo que antes
  (`{ registers: Register[], loading, error, openSession, closeSession, refresh }`), pero
  **`refresh()` ya no acepta argumentos**: `refresh(): Promise<void>`. Sigue devolviendo la
  promesa (los catch de open/closeSession la esperan antes de re-lanzar).
- `CajaPage` mantiene sus listeners de `focus`/`visibilitychange`, ahora llamando `refresh()`
  sin argumentos. **No están migrados al canal de frescura todavía** — eso es T-044.
- Tests: el mount se aserta con `expect(getRegisters).toHaveBeenCalledWith('loc1')` (un solo
  argumento). El helper `forceCalls()` de `useRegister.test.tsx` desapareció; los casos que
  contaban refrescos cuentan llamadas totales.

## Archivos
- Modificado: `src/features/register/data/register.repository.ts`
- Modificado: `src/features/register/application/useRegister.ts`
- Modificado: `src/features/register/application/useRegister.test.tsx`
- Modificado: `src/features/register/presentation/CajaPage.tsx`
- Modificado: `src/test/mocks/repositories.ts`
- Modificado (tolerancia de test): `src/features/register/presentation/CajaPage.test.tsx`
- Modificado (overlap declarado): `.harness/handoffs/T-004.md`, `T-012.md`, `T-013.md`,
  `T-026.md`, `T-029.md`

## Decisiones tomadas
- **Sin cambio de contrato de estado**: `registers` sigue siendo `Register[]` y arranca en `[]`.
  El "no sé" (`null` + status, esqueleto, [D-020]) y el registro en el canal de frescura llegan
  en T-044; esta tarea es sólo la mitad de datos.
- **Las evicciones se quedan** aunque el propio repositorio ya no lea del caché: quien sí lee
  `registers`/`posCajaStatusHome` del store es el gate del shell (`useCajaGate`) y Hoy; borrarlas
  les dejaría el snapshot pre-open.
- Los comentarios que justificaban la política vieja se reescribieron apuntando a la clase del
  dato en `core/apollo/dataClasses.ts` (mismo docblock que `day-sales.repository.ts`).

## Advertencias para dependientes
- **Cada `refresh()` es un viaje a red.** `CajaPage` refresca en mount + `focus` +
  `visibilitychange`, y `useCheckout` relee caja al cobrar y en `REGISTER_SESSION_STALE`. Un test
  con `MockedProvider` necesita `maxUsageCount > 1` para `PosRegisters` o falla en el segundo
  refresco ([T-015]).
- **No vuelvas a agregar un `opts`** al repositorio: si algo "parpadea", la salida es el estado de
  la pantalla (esqueleto/updating), no servir una caja vieja ([D-017], [D-018]).
- Al quitar los listeners en T-044 (registro en el canal, tema `'register'`), el caso
  `refetches registers on mount, window focus and visibilitychange` de `CajaPage.test.tsx` se
  reescribe con `announce('register')` del helper (T-016a).
- **Deuda fuera de alcance:** `src/features/checkout/data/checkout.repository.ts:1342` y
  `useCheckout.ts:~359` conservan comentarios que describen `getRegisters` como servido del
  caché; ya no es cierto. `src/features/register/presentation/steps/ConfirmDigitalStep.tsx`
  arrastra un error de lint pre-existente (por eso AC-5 se acotó a los archivos tocados) y
  `useRegister.ts:30` un warning `react-hooks/set-state-in-effect` idéntico al de HEAD
  (verificado contra la versión commiteada).
