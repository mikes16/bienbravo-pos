# Handoff T-003 — Sincronizar schema del API y regenerar tipos GraphQL (staffWorkingWindows)

## Interfaz expuesta
- Tipo generado `StaffWorkingWindow` en `src/core/graphql/generated/graphql.ts`: `{ __typename?: 'StaffWorkingWindow'; startMin: number; endMin: number }`.
- Query del schema disponible para consumir: `staffWorkingWindows(staffUserId: ID!, locationId: ID!, date: String!): [StaffWorkingWindow!]!` (tipo `QueryStaffWorkingWindowsArgs` también generado).
- No se generó `StaffWorkingWindowsQuery`/`Document` porque aún no existe ningún `graphql(\`query...\`)` en `src/**` que la use — eso lo agrega el consumidor (p. ej. T-004) usando `graphql()` de `@/core/graphql/generated`, lo cual regenerará esos tipos vía `npm run codegen`.

## Archivos
- Modificado: `schema.graphql`
- Modificado: `src/core/graphql/generated/graphql.ts`

## Decisiones tomadas
- Ninguna.

## Advertencias para dependientes
- Al escribir la query real, correr `npm run codegen` de nuevo para que se generen `StaffWorkingWindowsQuery`/`StaffWorkingWindowsDocument`; los archivos `gql.ts`, `index.ts`, `fragment-masking.ts` no cambiaron en este intento porque no hubo documentos nuevos que registrar.
- **Actualizado por T-019:** nueva sincronización de `schema.graphql`/`graphql.ts` (trae `posDataChanged`/`PosDataEventKind`/`PosSettings` y adiciones de dashboard/payroll/register no relacionadas). `StaffWorkingWindow` y `QueryStaffWorkingWindowsArgs` no cambiaron: el tipo sigue siendo `{ __typename?: 'StaffWorkingWindow'; startMin: number; endMin: number }`.
- **Actualizado por T-020:** nuevo `npm run codegen` por la subscription `PosDataChanged`; toca `graphql.ts` y, esta vez sí, `gql.ts`. Solo agrega ese documento: `StaffWorkingWindow` y `QueryStaffWorkingWindowsArgs` siguen idénticos.
- **Actualizado por T-022:** otro `npm run codegen`, ahora por la query `PosSettings` (ajustes de bloqueo del POS, `src/core/bootstrap/posSettings.queries.ts`); vuelve a tocar `graphql.ts` y `gql.ts`. Solo agrega ese documento: `StaffWorkingWindow` y `QueryStaffWorkingWindowsArgs` siguen idénticos.
- **Actualizado por T-028:** re-sync de `schema.graphql` (venta a staff: `staffSaleQuota`,
  `Sale.buyerStaffUserId`, `SaleItem.productVariantId`, `Product.staffSaleEligible` /
  `staffPriceResolvedCents`, `ProductVariant.staffPriceResolvedCents`) — **sin diff**: el API ya los
  traía desde antes de esta tarea. `StaffWorkingWindow` y `QueryStaffWorkingWindowsArgs` siguen
  idénticos. El único cambio real en `graphql.ts`/`gql.ts` es el documento `Seed` de
  `checkout.repository.test.ts`: `npm run codegen` llevaba tiempo saliendo con exit 1 (una query de
  prueba sin los args obligatorios de `posDaySales`) y T-028 lo corrigió para poder verificar drift.
