# Handoff T-019 — Sincronizar schema del API: avisos de datos y ajustes del POS

## Interfaz expuesta
- `Subscription.posDataChanged(slug: String!): PosDataEvent!` disponible en `schema.graphql`; tipos generados en `src/core/graphql/generated/graphql.ts`: `PosDataEvent { kind: PosDataEventKind!; locationSlug: string; occurredAt: string }` y `enum PosDataEventKind { Catalog, Commission, Payment, Register, Settings }`.
- `Query.posSettings: PosSettings!` disponible; tipo generado `PosSettings { posAutoLockIdleSeconds: number; posAutoLockCheckoutSeconds: number }`.
- No se agregó ningún documento `graphql()` en esta tarea. **Actualizado por T-020:** ya existe `POS_DATA_CHANGED` en `src/core/freshness/freshness.queries.ts`, así que `PosDataChangedSubscription`/`PosDataChangedSubscriptionVariables`/`PosDataChangedDocument` están generados y registrados en `gql.ts`. **Actualizado por T-022:** `PosSettings` ya tiene documento (`POS_SETTINGS` en `src/core/bootstrap/posSettings.queries.ts`, operación `PosSettings` sin variables), así que `PosSettingsQuery`/`PosSettingsQueryVariables`/`PosSettingsDocument` están generados y registrados en `gql.ts`.
- De paso, el sync trajo adiciones NO relacionadas ya presentes en el API (no pedidas por esta tarea, solo sincronizadas): campos nuevos en `DashboardLocationRow`/`DashboardTotals` (descuentos, `paidSalesCount`, `ticketPromedioCents`), `PayrollPreviewRow` (split de comisión producto/servicio/propinas + `totalCommissionCents`, `commissionCents` ahora `@deprecated`), y `RegisterSessionAdjustmentKind.REFUND`. Ningún tipo/campo existente fue eliminado.
- **Actualizado por T-028:** nuevo `npm run sync-schema` — **sin diff en `schema.graphql`**: el bloque de
  venta a staff (`staffSaleQuota`, `Sale.buyerStaffUserId`, `SaleItem.productVariantId`,
  `Product.staffSaleEligible`/`staffPriceResolvedCents`, `ProductVariant.staffPriceResolvedCents`) ya
  estaba incluido en el schema que T-019 sincronizó. `PosDataEvent`/`PosDataEventKind`/`PosSettings` no
  cambiaron de forma. El único diff real de esta tarea en `graphql.ts` es el documento `Seed` de
  `checkout.repository.test.ts` (le faltaban args obligatorios de `posDaySales`; `npm run codegen`
  salía con exit 1 desde antes de T-028, aunque igual escribía los archivos generados).

## Archivos
- Modificado: `schema.graphql`
- Modificado: `src/core/graphql/generated/graphql.ts`
- Sin cambios en esta tarea: `src/core/graphql/generated/gql.ts` (no hubo documentos nuevos que registrar; T-020 ya registró ahí `PosDataChanged` y T-022 `PosSettings`)

## Decisiones tomadas
- Ninguna.

## Advertencias para dependientes
- Al escribir la query/subscription real (canal de frescura, ajustes de auto-lock), correr `npm run codegen` de nuevo: se generarán `PosSettingsQuery`/`PosDataChangedSubscription`/`Document` y sí tocarán `gql.ts`. **Ya hecho:** T-020 (subscription) y T-022 (query de ajustes); ambos documentos existen y `gql.ts` los registra.
- `commissionCents` en `PayrollPreviewRow` quedó `@deprecated` (mismo valor, usa `totalCommissionCents`); si algo del POS lo lee, migrar en su propia tarea.
